{
  "name": "pranksters-inc",
  "version": "1.0.0",
  "description": "Web service for pranking your friends",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "cd server && npm i && cd ../client && npm i",
    "dev": "concurrently \"cd server && nodemon server.js\" \"cd client && npm start\"",
    "build": "cd client && npm run build",
    "start": "npm run build && cd server && cd node seeders/seed.js && node server.js",
    "server": "cd server && nodemon server.js",
    "client": "cd client && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^2.0.22"
  }
}
